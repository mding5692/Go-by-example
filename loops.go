package main 

import "fmt"

func main {
// creating a for loop
  // as you can see, no brackets needed
  // but one still needs semi-colons to separate it up
  for i := 0; i <= 5; i++ {
    fmt.Println(i)
  }


}
